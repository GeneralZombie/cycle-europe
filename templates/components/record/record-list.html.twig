<div class="container-fluid container-lg">
    <div class="row">
        <div class="col-12 col-md-6">
            <h3 class="bg-quarternary text-center">Distanz</h3>
            <div class="row">
                {%- for record in recordsDistance -%}
                    <div class="col-12">
                        {% include 'components/record/record-list-item.html.twig' with {
                            record: record
                        } only %}
                    </div>
                {%- endfor -%}
            </div>
        </div>
        <div class="col-12 col-md-6">
            <h3 class="bg-quarternary text-center">Steigung</h3>
            <div class="row">
                {%- for record in recordsAltitude -%}
                    <div class="col-12">
                        {% include 'components/record/record-list-item.html.twig' with {
                            record: record
                        } only %}
                    </div>
                {%- endfor -%}
            </div>
        </div>
    </div>
</div>